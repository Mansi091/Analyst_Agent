[project]
name = "analyst-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mansi091", email = "madgulemansi987@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "groq>=0.37.1",
    "langchain>=1.3.15",
    "langchain-groq>=1.1.3",
    "langgraph>=1.2.11",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
]

[project.scripts]
analyst-agent = "analyst_agent:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
